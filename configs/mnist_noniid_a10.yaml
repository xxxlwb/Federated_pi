# E4 Non-IID 接近 IID: alpha=1.0
exp_name: mnist_noniid_a10
dataset: mnist
model: mnist_cnn
num_classes: 10

num_clients: 2
num_rounds: 15
local_epochs: 1
batch_size: 32
learning_rate: 0.01

partition:
  strategy: dirichlet
  alpha: 1.0

server_address: "0.0.0.0:8081"
seed: 42
device: cpu
